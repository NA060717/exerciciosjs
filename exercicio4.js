/*
Crie um variável preço e atribua a ela um valor numérico. Em seguida, crie um variável desconto e atribua a ela um valor 0.2, representando a porcentagem (20%) de desconto que você deseja aplicar. Por fim, crie uma variável Preço final que use as variáveis preço e desconto para calcular o preço com desconto e imprima o resultado no console.
*/
let preco = 17;
let desconto = 0.2;
let soma = preco - (desconto * preco);

console.log(`Preço: ${preco}\nDesconto: 20%\nPreço Final: ${soma}`);